// Generated by ReScript, PLEASE EDIT WITH CARE

import type * as rescript from "@rescript/runtime/types";

export type opt<A> = rescript.option<A>;

export interface container<Hash, Eq, C> {
  size: number;
  buckets: opt<C>[];
  readonly hash: Hash;
  readonly eq: Eq;
}

export const emptyOpt: rescript.option<any>;

export function power_2_above(x: number, n: number): number;

export function make<A, B, C>(
  hash: A,
  eq: B,
  hintSize: number,
): container<A, B, C>;

export function clear<A, B, C>(h: container<A, B, C>): void;

export function isEmpty<A, B, C>(h: container<A, B, C>): boolean;