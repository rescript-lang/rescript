# JSX version 4

This path is retained because older announcements and discussions link to it.
The previous document mixed the current transform with obsolete migration and
configuration material.

- For current language usage and configuration, see the
  [JSX configuration manual](https://rescript-lang.org/docs/manual/build-configuration/#jsx)
  and the [ReScript React documentation](https://rescript-lang.org/docs/react/beyond-jsx/).
- For migration from older ReScript releases, see the
  [version 12 migration guide](https://rescript-lang.org/docs/manual/migrate-to-v12/).
- For compiler implementation details, see
  [`compiler/syntax/JSX.md`](../compiler/syntax/JSX.md).
