// Generated by ReScript, PLEASE EDIT WITH CARE

export interface Showable<T> {
  readonly show: (arg0: T) => string;
}

export interface Comparable<Identity, T> {
  readonly compare: (arg0: T, arg1: T) => number;
}

export interface Hashable<Identity, T> {
  readonly hash: (arg0: T) => number;
  readonly eq: (arg0: T, arg1: T) => boolean;
}

export type showable<A> = Showable<A>;

export type comparable<Key, Id> = Comparable<Id, Key>;

export type hashable<Key, Id> = Hashable<Id, Key>;

function MakeShowable(M) {
  return M;
}

function MakeComparable(M) {
  return M;
}

function MakePair(Left) {
  return Right => {
    let showPair = (l: Left.t, r: Right.t): string => "(" + Left.show(l) + ", " + Right.show(r) + ")";
    return {
      showPair: showPair
    };
  };
}

function MakeTriple(A) {
  return B => (C => {
    let showTriple = (x: A.t, y: B.t, z: C.t): string => "(" + A.show(x) + ", " + B.show(y) + ", " + C.show(z) + ")";
    return {
      showTriple: showTriple
    };
  });
}

function showable<A>(show: (arg0: A) => string): any {
  return {
    show: show
  };
}

function comparable<A>(compare: (arg0: A, arg1: A) => number): any {
  return {
    compare: compare
  };
}

function hashable<A>(hash: (arg0: A) => number, eq: (arg0: A, arg1: A) => boolean): any {
  return {
    hash: hash,
    eq: eq
  };
}

function show<A>(s: A): A {
  return s;
}

let stringShowable: any = {
  show: show
};

let intComparable: any = {
  compare: (a: number, b: number): number => a - b | 0
};

function useShowable<A>(m: any, x: A): string {
  return m.show(x);
}

export {
  MakeShowable,
  MakeComparable,
  MakePair,
  MakeTriple,
  showable,
  comparable,
  hashable,
  stringShowable,
  intComparable,
  useShowable,
}
/* No side effect */
