// Generated by ReScript, PLEASE EDIT WITH CARE


export interface record {
  readonly foo: string;
  readonly bar: number;
}

function add(a: number, b: number): number {
  return a + b | 0;
}

function greet(name: string): string {
  return "Hello, " + name;
}

function identity<A>(x: A): A {
  return x;
}

async function asyncIdentity<A>(x: A): Promise<A> {
  return x;
}

function processOption(opt: number | undefined): number {
  if (opt !== undefined) {
    return opt;
  } else {
    return 0;
  }
}

let myNumber = 42;

let myString = "hello";

export {
  add,
  greet,
  identity,
  asyncIdentity,
  processOption,
  myNumber,
  myString,
}
/* No side effect */
