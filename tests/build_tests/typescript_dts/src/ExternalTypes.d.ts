// Generated by ReScript, PLEASE EDIT WITH CARE

import type * as rescript from "@rescript/runtime/types";
import type $config from "mock-default";
import type { Observable, Subscription } from "mock-named";
import type * as $api from "mock-namespace";

export type mySet<A> = Set<A>;

export type myMap<K, V> = Map<K, V>;

export type myIterator<A> = Iterator<A>;

export type observable<A> = Observable<A>;

export type subscriptionShape = rescript.external<
  Subscription,
  {
    readonly unsubscribe: () => void;
    readonly closed: boolean;
  }
>;

export type subscription = rescript.external<
  Subscription,
  {
    readonly unsubscribe: () => void;
    readonly closed: boolean;
  },
  true
>;

export type config = typeof $config;

export type api = typeof $api;

export function makeSet(): mySet<string>;

export function makeMap(): myMap<string, number>;

export function getIterator<A>(_arr: A[]): myIterator<A>;

export function makeObservable(): observable<string>;

export function testSubscribe(obs: observable<string>): subscription;

export function testForEach(obs: observable<number>): Promise<void>;

export function testToPromise(obs: observable<number>): Promise<void>;