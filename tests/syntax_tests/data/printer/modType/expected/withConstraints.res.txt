module type A = Foo with type t = string

module type A = Foo with type Bar.t = string

module type A = Foo with type t = string

module type A = Foo with type t = string

module type A = Foo with type t = 'st

module type A = Foo with type t = 'st

module type A = Foo with type t = 'st and type t = 'st and type t = 'st

module type A = Foo with type t := string

module type A = Foo with type t := string

module type A = Foo with type t := string

module type A = Foo with type Bar.t := string

module type A = Foo with type Bar.t := string and type Bar.t := string and type Bar.t := string

module type A = Foo with module Bar = Array

module type A = Foo with module Bar = Belt.Array

module type A = Foo with module X.Bar = Belt.Array

module type A = Foo with module X.Bar = Belt.Array and module X.Bar = Belt.Array and module X.Bar = Belt.Array

module type A = Foo with module Bar := Array

module type A = Foo with module Bar := Belt.Array

module type A = Foo with module X.Bar := Belt.Array and module X.Bar := Belt.Array and module X.Bar := Belt.Array

module Same = (Na: N) => (Nb: N) => ({
  type number1 = Na.number
  type number2 = Nb.number
  let rec sim = ((n, m)) => if Na.is_zero(n) {
    Nb.is_zero(m)
  } else {
    sim((Na.pred(n), Nb.pred(m)))
  }
  let similar = ((n, m)) => try sim((n, m)) catch {
    | Na.Too_small => false
    | Nb.Too_small => false
  }
}: (S with type number1 = Na.number) with type number2 = Nb.number)
