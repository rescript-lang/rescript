let force_gc: unit => unit

type t = string

type t = string
and s = int

type Foo.Bar.t +=
| Foo
| Bar

type Foo.Bar.t +=
| Foo
| Bar

type Foo.Bar.t +=
| Bar

type t +=
| Foo = Bar

exception Exit = Terminate

exception ExitEarly(int, int)

exception ExitEarly(int, int): gadt

exception ExitEarly{code: int, timeout: int}: gadt

module X: MT

module rec X1: MT1
and Xn: MTn

module type S = MT

module type S

open Belt

include {
  let a: int
}

@@attr(payload)

@@extension(payload)
