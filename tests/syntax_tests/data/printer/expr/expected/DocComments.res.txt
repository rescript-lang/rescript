let doc1: (@res.doc(" ddd ") ~x: int) => int = (~x) => x + 1

let doc2: @res.doc(" ddd ") int => int = x => x + 1

let doc3: @res.doc(" ddd ") int => int = x => x + 1

let doc4: (@res.doc(" ddd ") ~x: int, @res.doc(" eee ") n) => int = (~x) => x + 1

module M = ({
  let foo = (~x, ~y) => x + y
}: {
  let foo: (@res.doc(" xxdoc ") ~x: int, @res.doc(" yydoc ") ~y: int) => int
})

@val external ex: (@res.doc(" ddd ") ~x: int, @res.doc(" eee ") n) => int = "ex"

module rec A = ({
  type t
}: {
  type t
})
and B = ({
  type t
}: {
  type t
})
and C = ({
  type t
}: {
  type t
})
