let someDict = Primitive_dict.make([("one", "one")])

let {one: ? one, _} = someDict

let foo = () => {
  let _ = switch someDict {
    | 
    // Comment here
    {
    // Comment above the pattern
    one: 
    // Comment right in the pattern
    "one", _} => Js.log("one")
    | _ => Js.log("not one")
  }
  let _ = switch someDict {
    | {
    // foo
    one: "one", 
    // bar
    twoooooooooo: 2, 
    // baz
    three: 3, fooooour: 4, fiiiive: 5, _} => Js.log("one")
    | _ => Js.log("not one")
  }
  let _ = switch someDict {
    | {
    /* foo */
    
    /* baz */
    one: "one", 
    /* bar */
    
    /* baz */
    twoooooooooo: 2, 
    /* baz */
    
    /* baz */
    three: 3, fooooour: 4, fiiiive: 5, _} => Js.log("one")
    | _ => Js.log("not one")
  }
  ()
}
