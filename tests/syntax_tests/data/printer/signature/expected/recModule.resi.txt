module type Signature = {
  module rec A: {
    type t =
    | Leaf(string)
    | Node(ASet.t)
    let compare: t => t => int
  }
  and ASet: Set.S with type elt = A.t
  and BTree: Btree.S with type elt = A.t
  module rec A: {
    type t =
    | Leaf(string)
    | Node(ASet.t)
    let compare: t => t => int
  }
  and ASet: Set.S with type elt = A.t
  module rec A: Btree
  and ASet: BSet
}
