type s = string

type user = {
  name: string,
  age: int
}

type color =
| Red
| Blue
| Black

type rec \"options.permissions" = {
  all: \"options.permissions.all"
}
and \"options.permissions.all" = {
  stuff: bool
}
and options = {
  permissions: \"options.permissions"
}
