module Device = (val ((let deviceName = (parseCmdline ()) in (try (Hashtbl.find devices deviceName) with | Not_found -> (exit 2))) : (module Device)))
let draw_using_device = (fun device_name -> (fun picture -> (let module Device = (val ((Hashtbl.find devices device_name) : (module DEVICE))) in (Device.draw picture))))
module New_three = (val (three : (module X_int)))
let to_int = (fun m -> (let module M = (val (m : (module X_int))) in M.x))