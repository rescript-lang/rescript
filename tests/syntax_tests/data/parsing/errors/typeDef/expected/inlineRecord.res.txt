type nonrec entity = Director | Student of {name: string; reportCard: <passing: bool; score: int>}
type user.address = {
  street: string ;
  country: string } and user = {
  name: string ;
  address: user.address }
let make = (fun (props : <handleClick: Click.t -> unit (a:1); value: string>) -> (render props))