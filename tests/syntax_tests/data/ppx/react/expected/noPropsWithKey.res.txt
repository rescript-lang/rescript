@@jsxConfig({version: 4})

module V4CA = {
  @res.jsxComponentProps
  type props = {}

  let make = (_: props): React.element => ReactDOM.jsx("div", {})
  @res.hoistedValue
  let make = React.component({
    let \"NoPropsWithKey$V4CA$make" = props => make(props)

    \"NoPropsWithKey$V4CA$make"
  })
}

module V4CB = {
  @res.jsxComponentProps @live
  type props = {}

  @res.hoistedValue @module("c")
  external make: React.component<props> = "component"
}

module V4C = {
  @res.jsxComponentProps
  type props = {}

  let make = (_: props): React.element =>
    React.jsxs(
      React.jsxFragment,
      {
        children: React.array([
          React.jsxKeyed(@res.jsxComponentPath V4CA.make, {}, ~key="k", ()),
          React.jsxKeyed(@res.jsxComponentPath V4CB.make, {}, ~key="k", ()),
        ]),
      },
    )
  @res.hoistedValue
  let make = React.component({
    let \"NoPropsWithKey$V4C$make" = props => make(props)

    \"NoPropsWithKey$V4C$make"
  })
}
@warning("-32") let \"NoPropsWithKey$V4CA$make" = V4CA.make
@warning("-32") let \"NoPropsWithKey$V4CB$make" = V4CB.make
@warning("-32") let \"NoPropsWithKey$V4C$make" = V4C.make
