@@jsxConfig({version: 4})

module C0 = {
  module M = {
    @res.jsxComponentProps
    type props = {}

    let make = (_: props): React.element => React.null
    @res.hoistedValue
    let make = React.component({
      let \"DefaultPatternProp$C0$M$make" = props => make(props)

      \"DefaultPatternProp$C0$M$make"
    })
  }

  module type S = module type of M
  @res.jsxComponentProps
  type props<'component> = {
    component?: 'component,
  }

  let make = ({component: ?__component, _}: props<_>) => {
    let __component_value = switch __component {
    | Some(component) => component
    | None => module(M: S)
    }
    let module(C: S) = __component_value
    (React.jsx(@res.jsxComponentPath C.make, {}): React.element)
  }
  @res.hoistedValue
  let make = React.component({
    let \"DefaultPatternProp$C0$make" = (props: props<_>) => make(props)

    \"DefaultPatternProp$C0$make"
  })
}
@warning("-32") let \"DefaultPatternProp$C0$M$make" = C0.M.make
@warning("-32") let \"DefaultPatternProp$C0$make" = C0.make
