@@jsxConfig({version: 4})

module V4A = {
  @res.jsxComponentProps
  type props<'a, 'b> = {
    a: 'a,
    b: 'b,
  }

  let make = (type a, {a, b, _}: props<_, _>): React.element => ReactDOM.jsx("div", {})
  @res.hoistedValue
  let make = React.component({
    let \"TypeConstraint$V4A$make" = (props: props<_>) => make(props)

    \"TypeConstraint$V4A$make"
  })
}
@warning("-32") let \"TypeConstraint$V4A$make" = V4A.make
