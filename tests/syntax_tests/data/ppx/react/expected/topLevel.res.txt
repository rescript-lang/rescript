@@jsxConfig({version: 4})

module V4A = {
  @res.jsxComponentProps
  type props<'a, 'b> = {
    a: 'a,
    b: 'b,
  }

  let make = ({a, b, _}: props<_, _>) => {
    Js.log("This function should be named 'TopLevel.react'")
    (ReactDOM.jsx("div", {}): React.element)
  }
  @res.hoistedValue
  let make = React.component({
    let \"TopLevel$V4A$make" = (props: props<_>) => make(props)

    \"TopLevel$V4A$make"
  })
}
@warning("-32") let \"TopLevel$V4A$make" = V4A.make
