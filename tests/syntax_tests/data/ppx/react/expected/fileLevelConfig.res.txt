@@jsxConfig({version: 4})

module V4A = {
  @res.jsxComponentProps
  type props<'msg> = {
    msg: 'msg,
  }

  let make = ({msg, _}: props<_>): React.element => {
    ReactDOM.jsx("div", {children: ?ReactDOM.someElement({msg->React.string})})
  }
  @res.hoistedValue
  let make = React.component({
    let \"FileLevelConfig$V4A$make" = (props: props<_>) => make(props)

    \"FileLevelConfig$V4A$make"
  })
}
@warning("-32") let \"FileLevelConfig$V4A$make" = V4A.make
