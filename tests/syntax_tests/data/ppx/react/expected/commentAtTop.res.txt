// test React JSX file
@res.jsxComponentProps
type props<'msg> = {
  msg: 'msg
}

let make = ({msg: msg, _}: props<_>): React.element => ReactDOM.jsx("div", {children: ? ReactDOM.someElement((msg -> React.string))})

let make = {
  let CommentAtTop = (props: props<_>) => make(props)
  CommentAtTop
}
