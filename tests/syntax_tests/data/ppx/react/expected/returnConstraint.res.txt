@@jsxConfig({version: 4})

module Standard = {
  @res.jsxComponentProps
  type props = {}

  let make = (_: props): React.element => React.string("ok")
  @res.hoistedValue
  let make = React.component({
    let \"ReturnConstraint$Standard$make" = props => make(props)

    \"ReturnConstraint$Standard$make"
  })
}

module ForwardRef = {
  @res.jsxComponentProps
  type props = {}

  let make = (_: props, _ref): React.element => ReactDOM.jsx("div", {})
  @res.hoistedValue
  let make = React.forwardRef({
    let \"ReturnConstraint$ForwardRef$make" = (props, ref) => make(props, ref)

    \"ReturnConstraint$ForwardRef$make"
  })
}

module WithProps = {
  type props = {value: int}

  let make = (props: props): React.element =>
    ReactDOM.jsx("span", {children: ?ReactDOM.someElement({React.int(props.value)})})
  @res.hoistedValue
  let make = React.component({
    let \"ReturnConstraint$WithProps$make" = (props: props): React.element => make(props)
    \"ReturnConstraint$WithProps$make"
  })
}

module Async = {
  @res.jsxComponentProps
  type props = {}

  let make = async (_: props): React.element =>
    ReactDOM.jsx("div", {children: ?ReactDOM.someElement({React.string("async")})})
  @res.hoistedValue
  let make = React.component({
    let \"ReturnConstraint$Async$make" = props => Jsx.promise(make(props))

    \"ReturnConstraint$Async$make"
  })
}
@warning("-32") let \"ReturnConstraint$Standard$make" = Standard.make
@warning("-32") let \"ReturnConstraint$ForwardRef$make" = ForwardRef.make
@warning("-32") let \"ReturnConstraint$WithProps$make" = WithProps.make
@warning("-32") let \"ReturnConstraint$Async$make" = Async.make
