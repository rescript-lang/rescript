module Outer = {
  @res.jsxComponentProps
  type props = {}
  let make = (_: props): React.element => {
    module Inner = {
      @res.jsxComponentProps
      type props = {}

      let make = (_: props): React.element => ReactDOM.jsx("div", {})
      @res.hoistedValue
      let make = React.component({
        let \"Nested$Outer$Inner$make" = props => make(props)

        \"Nested$Outer$Inner$make"
      })
    }

    React.jsx(@res.jsxComponentPath Inner.make, {})
  }
  @res.hoistedValue
  let make = React.component({
    let \"Nested$Outer$make" = props => make(props)
    \"Nested$Outer$make"
  })
}
@warning("-32") let \"Nested$Outer$Inner$make" = Outer.Inner.make
@warning("-32") let \"Nested$Outer$make" = Outer.make
