module Outer = {
  @res.jsxComponentProps
  type props = {}
  let make = (_: props): React.element => {
    module Inner = {
      @res.jsxComponentProps
      type props = {}

      let make = (_: props): React.element => ReactDOM.jsx("div", {})
      let make = {
        let \"Nested$Outer$Inner" = props => make(props)

        \"Nested$Outer$Inner"
      }
    }

    React.jsx(@res.jsxComponentPath Inner.make, {})
  }
  let make = {
    let \"Nested$Outer" = props => make(props)
    \"Nested$Outer"
  }
}
let \"Nested$Outer$Inner" = Outer.Inner.make and \"Nested$Outer$Inner$jsx" = true
let \"Nested$Outer" = Outer.make and \"Nested$Outer$jsx" = true
