module A = {
  @res.jsxComponentProps
  type props<'x> = {
    x: 'x,
  }
  let make = ({x, _}: props<_>): React.element => React.string(x)
  @res.hoistedValue
  let make = React.component({
    let \"Interface$A$make" = (props: props<_>) => make(props)
    \"Interface$A$make"
  })
}

module NoProps = {
  @res.jsxComponentProps
  type props = {}

  let make = (_: props): React.element => ReactDOM.jsx("div", {})
  @res.hoistedValue
  let make = React.component({
    let \"Interface$NoProps$make" = props => make(props)

    \"Interface$NoProps$make"
  })
}
@warning("-32") let \"Interface$A$make" = A.make
@warning("-32") let \"Interface$NoProps$make" = NoProps.make
